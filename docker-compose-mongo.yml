version: '3.8'
services:
  mongo:
    image: mongo:6
    container_name: mongo-db
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: imageroot
      MONGO_INITDB_ROOT_PASSWORD: imageroot321.

volumes:
  mongo_data:

